name: Block direct push to master

on:
  push:
    branches:
      - master

jobs:
  reject-direct-push:
    runs-on: ubuntu-latest
    steps:
      - name: Fail direct push to protected branch
        run: |
          echo "Direct pushes to master are not allowed. Please open a pull request and merge via GitHub review."
          exit 1
